function cookMaggi(maggi, water, pot) {
    console.log(
      `your maggi will be ready in ${
        maggi * 2
      } minutes and the ingredients used are ${maggi} packets of maggi, ${water} ltr of water and ${pot} pot`
    );
  }
  cookMaggi(2,1,1)